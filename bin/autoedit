#!/bin/env bash

files="/home/bart/Videos/raw/"
for f in "$files"*; do
  # I have to define the h264 video codec because it fails converting GoPro videos without this option.
  auto-editor --no-open --margin 0.2sec --video-codec h264 "$f"
done
